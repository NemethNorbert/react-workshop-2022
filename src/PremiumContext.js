import React from 'react';
let PremiumContext;
const {Provider, Consumer} = PremiumContext = React.createContext({});

export {Provider as PremiumProvider, Consumer as PremiumConsumer, PremiumContext};